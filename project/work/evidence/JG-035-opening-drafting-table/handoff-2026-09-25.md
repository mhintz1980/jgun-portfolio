# JG-035 session handoff — 2026-09-25

Session: Claude Code (d4a4cbdf), branch `codex/jg033-signature-shot`. For the next agent picking up JG-035.

## Goals this session (all owner-directed 2026-09-24/25)

1. **Restyle the intro drawing** to the owner's cream-vellum/navy-ink reference while keeping the
   lift-out registered to the live GLB profile (the linework IS the model — no image asset).
2. **Oversized-sheet intro (Track A):** sheet ~3× viewport that the camera READS by panning,
   superseding the fit-to-screen framing.
3. **Tolerance stations (Track B):** replace CH.01's decorative GD&T leaders with 5–6 stations
   carrying the owner's REAL held tolerances and process stories. Facts locked in the fact sheet.
4. **Stage 1 gate:** still frames of both tracks for owner + Astra visual ruling BEFORE any
   animation build (repo rule: visual effects need Astra approval; owner rules by looking).

## What was done (chronological, with commits)

| Commit | What |
|---|---|
| `2c21415` | Cream paper `#d8d1bd` + navy ink `#142a52` (`PAPER`/`INK` in drawingGeometry.ts); graph grid, lettered zone frame, centring marks, reference-style title block / revision table / general notes in the SVG overlay. Overlay colors now import the constants. |
| `55f679b` | Fact sheet v2 — FACTS LOCKED (see below). |
| `aceba9e` | JG-035 triaged: registry row in `project/work/INDEX.md` + TODO entry. |
| `e125f03` | Oversized sheet: `SHEET_HEIGHT` 0.7 → **2.0** (width 2.588, ANSI C 22:17); `PRINT_TARGET_*` 1760×1360 → **3520×2720**; `SHEET_ZONES` furniture pushed to new edges (notes column now left rail); grid re-pitched 25/100 mm-equiv; detail band (DETAIL B/C/D circles + hatched sample) + tolerance strip (inches) added to the overlay; `sheetCamera.ts` desktop gets the reading path (title-block open t=0 → full-sheet t≈0.14 → views pan → settle at `INTRO_PHASES.pulseStart`); blob-URL retry in `__drawingSvgReady`; capture script waits for `annotationsReady`. |

Stage-1 stills (NOT committed — they are scratch evidence): `.scratch/drawing-sheet-oversized/`
(4 frames: t=0/0.03/0.06/0.1) and `.scratch/station-stills/` (station-S2-p52{,-zoom}.png,
station-S4-p5.png + verification.json, all PASS). Sent to the owner in-session; **owner and
Astra rulings still pending**.

## The locked facts (source of truth: fact sheet v2)

`project/work/inbox/JG-035-tolerance-stations-facts.md` — do not re-ask the owner these:

- Units **inches** everywhere (G1). The intro sheet's "MILLIMETERS" note + mm-style frames must
  flip to inches — **not yet done**, do it in the animation build.
- Geared runout **< .001" TIR** replaces every `.0015"` string (`caseStudies.ts` CH.01/02/04) — **not yet done**.
- **ISO 1328 / AGMA 2015 Grade A6**, all gears cut IN HOUSE (pinion-wire story dropped; class-10 dropped).
- Datums: A = drivetrain axis via **P003069 journals in P000245's ID**; B = mount face;
  C = output face; D = planet bore/pin; E = shifter-fork end face.
- S2 shafts (P001836 sun + P000095 output): total runout `.001 | A-B`, single chucking.
- S3 planets: circular runout `.001 | D` + ISO 1328 A6.
- S4 shifter fork P000724 (+cam P000297): profile `.004 | A E` = .004 total zone, ±.002/side.
- S5 clutch housing **P000420** OD into P000245 bore: `⌀2.525" H7/k6` + `.001 | A`, "finished
  after heat treat". P000420 displays as "Clutch Housing"; P000297 as "Shifter Cam".
- S6 summary card kept. CH.02/CH.03 hotspots untouched; rotor "BALANCED VANE" note kept;
  flange flatness .0008 survives inside S1.

## Key docs / where things live

- **Fact sheet (content authority):** `project/work/inbox/JG-035-tolerance-stations-facts.md`
- **Registry row + state protocol:** `project/work/INDEX.md` (JG-035 row, top)
- **Queue entry:** `TODO.md` (Active, top)
- **Repo rules:** `AGENTS.md`, `project/README.md` (work-ID protocol), read-first
  `project/context/architecture/animation-spec.md` §5–5.4
- **JG-034 bug (rides this build):** `project/work/evidence/JG-034-opening-scene-refit/report-2026-09-22.md`
  — sheet wrong/missing until first viewport resize; suspect stale R3F `size` at
  `DrawingLinework.tsx` mount (~line 179). Observed again during this session's captures.
- Code touched: `src/scene/drawing/{drawingGeometry,sheetCamera,DrawingLinework}.ts*`,
  `src/components/EngineeringDrawingOverlay.tsx`, `scripts/capture-drawing-sheet.mjs`.
- Capture tooling: `scripts/capture-drawing-sheet.mjs` (usage `node scripts/capture-drawing-sheet.mjs [base] [outDir]`);
  probes in `.scratch/probe-*.mjs`; station compose harness in `.scratch/station-stills/`
  (compose.html + shoot.mjs; needs `--allow-file-access-from-files`, already set).

## Known issues / what to correct

1. **Stills are design drafts, not final.** The detail-band circles are empty circles (no 2:1
   GLB renders inside yet — placeholder convention only); the GD&T strip is a static table, not
   the scroll-driven 3D stations. Treat them as look-approval artifacts only.
2. **Sheet visual density is still being tuned** — owner has not ruled. Expect requests to
   adjust grid opacity, type sizes, station framing.
3. **JG-034** unfixed (see above) — must land with the animation build.
4. **G1/G2 copy sweep** (inches note, .0015 replacement) still open.
5. `sheetCamera.ts` retains `layout.narrow`/`fitDistance` fields but the narrow branch is now
   dead code path-wise (`void layout.narrow`); tidy when JG-034 work touches the layout path.
6. Other sessions are actively editing `src/scene/rl300/*` in this same checkout (uncommitted
   changes present — do NOT commit or revert them; commit only your files explicitly).
7. The tolerance strip's ⌀/AGMA glyph cells are TEXT, not the real GdtSymbol SVGs — swap in
   `src/components/GdtSymbols.tsx` paths when building for real.

## Non-obvious environment facts (cost real time)

- The annotation pipeline (model load + 3520×2720 SVG raster) takes **~16 s**; wait for
  `telemetry.drawing.annotationsReady`, never a fixed timeout.
- The desktop-app Browser pane does NOT run rAF when idle — screenshots there show stale frames.
  Use headless Playwright (`channel: 'chrome'`) via scripts in `.scratch/`.
- Playwright bare `chromium.launch()` dies (version mismatch) — always `channel: 'chrome'`.
- A subagent run for station stills was killed by a gateway 502 (weekly limit); its partial
  work in `.scratch/station-stills/` was finished and verified by the main session. Its
  worktree (`../jgun-station-stills`) was removed.

## Next steps (in order)

1. Collect owner + Astra rulings on the seven stills (sheet look; gauge/tolerance-band props;
   FCF build pace; navy-on-dark contrast).
2. Fold rulings into the fact-sheet §4 answers; then write the JG-035 plan
   (`project/work/plans/JG-035-*.md`) covering: detail-circle 2:1 renders, real GD&T glyphs,
   the inches copy sweep, station choreography in CH.01, JG-034 fix riding along.
3. Animation build → evidence per repo gate rules (telemetry, not vision, for machine checks;
   owner visual ruling to close).
