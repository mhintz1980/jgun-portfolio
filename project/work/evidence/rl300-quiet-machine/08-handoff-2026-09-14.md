# Handoff — 2026-09-14: multi-vendor lanes are live, JG-033 Milestone 3 is next

For the next session. Astra stopped mid-JG-033 on a rate limit; its last writes to this
repo are dated 2026-09-12 05:15. Nothing was lost — Milestone 2 is ruled, gated, and
committed. What changed since is **infrastructure**, not scene work.

Read this, then [`07-handoff-for-astra.md`](07-handoff-for-astra.md) for the geometry
rulings it supersedes nothing of.

---

## The intended working shape

Mark's instruction for the next several tasks:

| Role | Who | Does what |
|---|---|---|
| Orchestrator | **Fable** | Plans the next steps, decomposes them, writes the six-part specs, delegates |
| Implementation (most) | **`deepseek-implementer`** | The bulk of the typing |
| Implementation (some) | **`glm-implementer`** | A smaller share, for vendor diversity |
| Review | **Claude (this session's role)** | Reviews *both* lanes' work before anything is called done |

The architect never implements. The reviewer never accepts a lane's own claim of success —
re-run the verification, read the diff, and prefer a mutation check on anything that
claims to be a test. See `~/.claude/skills/orchestration/SKILL.md` for the routing
doctrine and the six-part spec format.

## The lanes, as of today

Five lanes exist; four are proven on a real task.

    deepseek-implementer   DeepSeek Flash, always High effort   cheapest, proven 2026-09-14
    glm-implementer        GLM-5.3, variant: max                proven
    codex-implementer      GPT-5.6 Luna, effort per task        proven, but see WARNING
    sol-implementer        GPT-5.6 Sol, up to `ultra`           never run a real task
    fable-advisor          Fable 5.1                            never exercised end-to-end

Cost ordering, cheapest first: `deepseek` -> `glm` -> `codex` -> `sol`.

**WARNING — Luna and Sol are unverified on codex 0.154.0.** Mark upgraded codex mid-session
on 2026-09-14 while the OpenAI quota was exhausted, so nothing confirms the upgrade left
the two codex lanes working. Isolation evidence says it should have (`--ignore-user-config`,
`--profile lane`) but that is inference, not a test. **Smoke both before routing real work
to them.**

Full lane mechanics, costs, and the traps live in
`C:\vaults\markimus-SecondBrain\06-AI-Agents\lane-fleet-handoff-2026-09-14.md`.

### Two lane facts that will bite if forgotten

- **DeepSeek runs unsandboxed** (`danger-full-access`). Codex has no Windows sandbox
  backend, so `workspace-write` fails closed and rejects even `echo ok`. Containment is
  the WORKDIR — **give it a git worktree, never the live repo.**
- **Neither cheap lane can prove its own served model.** Report models as *requested, not
  confirmed*. For GLM the per-request oracle is `~/.zcode/cli/db/db.sqlite` -> `model_usage`;
  for DeepSeek there is no local record at all, only Mark's usage dashboard.

## What this session changed in this repo

One commit: `4f6f619` — `test: characterize stageWindows scroll-window maths`.

21 characterization tests for `src/scene/stages/stageWindows.ts`, which had **zero**
coverage despite StageManager and AirflowField both depending on its numbers. Written by
the DeepSeek lane in an isolated worktree, cost **$0.045**, reviewed here: 21/21 pass,
`tsc --noEmit` clean, and two independent mutations bite (active gate `0.001 -> 0` fails 2
tests; smoothstep -> linear fails 6). No production file touched.

Test count went **55 -> 76**. This was a lane-proving exercise chosen because it was
genuinely useful and carried no design judgment — it is not JG-033 work.

## JG-033 — where it actually stands

**Milestone 2 is done and ruled.** 24/24 ruled occurrences PASS. `npm run typecheck`,
`npm test`, `npm run build`, `npm run check:station2` all PASS. 1440x900, 768x1024, 390x844
full and lite: 0 page errors, 0 console errors. Forward/reverse pixel-identical.

**Milestone 3 is the next work, and it is untouched.** From the plan's build order, step 3:
add the seven shots, RL300-local progress, unrestricted documented scroll extension,
chapter/HUD synchronisation, and the editorial overlay, plus the specific JGUN timing
corrections.

`src/scene/rl300/shot.ts` is **still a three-beat preview camera**, not the seven-shot
sequence. That is the single biggest gap. The seven shots and their proposed RL300-local
`u` ranges are tabulated in
[`../../plans/JG-033-rl300-quiet-machine.md`](../../plans/JG-033-rl300-quiet-machine.md)
under "Shot plan" — 01 The object (0–.12) through 07 Resolve (.89–1). Those `u` values are
RL300-local progress, **not** replacements for global progress or JGUN timing.

### Still open, carried forward from Milestone 2

- Composer stencil target and MSAA resolve
- Production handoff; the old lite-tier cost anomaly
- The 250k-triangle lite geometry budget (lite still only cuts DPR and shadows)
- Real mobile GPU behaviour; full accessibility audit; complete context restoration
- Manufacturing collisions
- **JG-032 contract migration — `scripts/verify-jg032-station2-thermal.mjs:462` is still
  stale-red at HEAD.** Good candidate for a cheap lane: narrow, mechanical, crisply
  verifiable.

### Pending Mark, not blocking

The **pump end is missing from the source export itself** — never in the enclosure GLB,
confirmed in his own assembly. He is exporting it from Onshape. When it lands: keep
**Y-up** (the placement chain has no rotation anywhere; a rotated import breaks the CAD
anchors), name parts by part number before export so `PART_POLICY` can target occurrences,
Draco via gltf-transform, and **never** `gltfjsx --transform` on this GLB.

## Open question — resolve before planning the shots

Mark recalls Astra having rendered an image with **"the job site background."**

**No such asset or background exists in this repo.** The three committed previews
(`public/images/rl300-{exterior,intake,section}-preview.png`) all render on the dark studio
void — a near-black gradient, a ground plane, and a soft pool of light. A repo-wide search
for `job.?site` returns nothing in `src/`, the plans, or the evidence.

So this is one of three things, and the next session must ask rather than guess:

1. Mark is describing the dark ground-plane-and-gradient environment as "the background";
2. he saw it in a session whose output never reached this repo; or
3. he wants a job-site environment **added** — which would be new art direction, not a
   continuation, and would need to be reconciled with the plan's "Art direction" section.

Do not invent a job-site background on the strength of this note.

## Verification commands

    npm run typecheck
    npm test                        # 76 tests, 8 files, all green at 4f6f619
    npm run build
    npm run check:station2
    node scripts/verify-jg033-preview.mjs

## Repo state at handoff

Branch `codex/jg033-signature-shot`, HEAD `4f6f619`. **29 uncommitted paths** in the
working tree — the in-flight JG-033 Milestone 2 material (RL300 scene sources, preview
PNGs, evidence docs, verify scripts) plus untracked `scripts/build-jg033-lite.py`,
`scripts/verify-jg033-lite-asset.mjs`, and `src/scene/sectionRenderPass.ts`. None of it was
touched this session; the test commit went in as a fast-forward that left all 29 intact.

`output/` is gitignored (12 MB of regenerable capture PNGs); that `.gitignore` change is
itself still uncommitted.
