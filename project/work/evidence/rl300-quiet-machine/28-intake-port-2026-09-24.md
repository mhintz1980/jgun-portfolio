# Evidence 28 — 2026-09-24: owner's intake move ported (handoff 27 §1)

## What landed

- `LowerIntake.tsx`: `group.position.set(0, .205, .15)` — the owner's measured pure translation.
- `flow.ts` `SPINES.lower` wp0–5 shifted by (0,+.205,+.150); wp6–8 unchanged.
- `preview.test.ts`: louver-gap and lower-bundle ruling constants shifted (start band y .075–.143,
  z .599–1.202; panel plane y .172; collector z .625–1.175 y .181–.343; duct z .114–.734 with
  panel bottom .334 / floor .196; riser z -.305…-.035, y ≤ .525, wall x -.204 unchanged).

## Correction to handoff 27

Handoff 27 listed shifted wp5 as `[-.17,.505,.005]`. -.300 + .150 = **-.150**; shipped
`[-.17, .505, -.150]` (inside the shifted riser band).

## Verification

- Scorer `.scratch/lower-fix/score-shift.mjs` (score.mjs with every intake constant offset by
  DY .205 / DZ .150): ALL 13 CHECKS PASS. Crossing (-.180, .930) in the shifted open gap;
  collector y .220–.315; duct y .281–.315; riser wall clearance 30 mm; handoff 0.29 mm;
  maxTurn 7.57°, minR 53 mm. Arc 1850 mm (was 1968 mm) — draw window `smooth(.41,.60,u)` kept.
- GLB probes `.scratch/lower-fix/measure-shift.mjs` against `msp-enclosure.glb` @15fd488 (v5,
  real pump): wp5 84.6 mm (CCM-0100), wp5–6 102/52 mm, wp6 19.1 mm (EMG-1001-P, unchanged
  waypoint), wp6–7 76.8 mm / wp7 52.3 mm / landing 91.5 mm (PUMP_HOUSING 26-01305). No contact.
- vitest rl300: 25/25. Mutation teeth: reverting either the spine or the group shift fails the
  lower-bundle ruling test.

## NOT done (time-boxed session)

No producer dispatch / adversarial review, no rebuild, no captures or contact sheet — dist on
:4173 is still 15fd488. Next: rebuild, restart preview, capture six stops + u .45–.65,
before/after pixdiff, contact sheet → owner ruling; then the Astra re-clear packet.

---

# Follow-up cycle (same day, owner review in browser)

Owner review of the live dev server produced three asks, all landed (uncommitted):

1. **Sectioned with the assembly.** Intake materials now clip on the same live plane
   (`m.clippingPlanes = [plane]`, DoubleSide, clipShadows); `SectionCaps` gained an `order`
   prop and the intake's two unions (louvers, collector/duct) get stencil caps ahead of the
   enclosure's (-60). `createLowerIntake(plane?)`; caps toggle follows `control.caps`.
2. **Engine-side move + four louvers.** Owner moved his Blender copy .120 toward the engine
   (−z) to clear the skid perimeter tube (measured: tube face z .747, louvers would have ended
   .746 — 1 mm clash). World offset now **(0, .205, .03)**. Louver count 9→**4** (owner
   ruling), spacing .061, frame trimmed to z .462–.733 (aft edge on the 1047-5 boundary).
   Collector shortened to match. Spine wp0–5 re-derived: [.682,.678,.540,.300,−.070,−.270];
   crossing z .687 mid-gap (.678–.697), scorer 13/13 (`.scratch/lower-fix/score-v3.mjs`,
   DY .205 DZ .030), arc 1648 mm. Tests re-pinned (start z .479–.777, collector .505–.750,
   duct −.006–.614, riser −.425…−.155). 25/25 + tsc clean; mutation teeth re-verified earlier
   pattern (old spine/old offset each fail the ruling test).
3. **Camera at stop 04** dropped below the panel to read the slats from underneath:
   key `at .51` → position [1.05, −.13, 1.05], target [−.2, .2, .6], fov 38 (portrait keeps
   y > −.22; the first try −.19 failed the envelope test in portrait ×1.22 pull-back).
   Visual change pending Astra per the standing rule.

Verified in browser at the Lower intake stop (1600×1000): camera under the frame, 4 slats
visible, clipped intake + caps, ribbon path visible. Console errors only pre-existing React
key warnings (duplicate loading-assy keys, unrelated to this work).

